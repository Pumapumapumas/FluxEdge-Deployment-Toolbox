#!/bin/bash

UPTIME_KUMA_URL="{{ uptime_kuma_url }}"
CHECK_INTERVAL="{{ uptime_kuma_interval | default(60) }}"  # Interval between heartbeats

echo "🚀 Starting Uptime Kuma Passive Monitor..."

while true; do
    curl -s "$UPTIME_KUMA_URL" && echo "✅ Heartbeat sent at $(date)"
    sleep $CHECK_INTERVAL
done
